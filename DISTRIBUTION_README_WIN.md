# びくたん Desktop（Windows版） — はじめにお読みください

## 動作環境

- Windows 10 / 11（64ビット・x64）
- 会話には、**ご自身で用意する会話AI**が必要です（無料で始められます。下記「2. 会話AIを決める」）
- 声には**VOICEVOX（無料）が必要です**。入れるまで、びくたんは文字だけで話します

## インストール

1. zipを展開し、フォルダごと好きな場所へ置きます（ドキュメントの中など）。
2. フォルダの中の `bikutan.exe` を実行します。
3. 「WindowsによってPCが保護されました」と表示されたら、「詳細情報」→「実行」を選びます。

本アプリはコード署名を受けていません。Microsoftによるマルウェア確認済みという意味ではないため、必ず配布元から直接受け取ったファイルを使い、配布時に示されたSHA-256と一致することを確認してください。

インストーラーはありません。フォルダを消せばアンインストールできます（後述）。

## 最初にすること

### 1. 通知領域のアイコンを見つける

**Windows 11は、入れたばかりの常駐アイコンを隠します。** 時計の左にある「^」を押すと出てくるので、そこから外へドラッグしておくと以後ラクです。

アイコンを**左クリック**するとメニューが開きます。設定もお願いごとも、すべてここからです。

### 2. 会話AIを決める（これをしないと会話できません）

メニュー →「🤖 会話AI」

| 選べるもの | 費用 | 用意するもの |
|---|---|---|
| **Gemini APIキー** | 無料枠あり | [Google AI Studio](https://aistudio.google.com/apikey) で発行したキー |
| Claude APIキー | 有料 | [Anthropic Console](https://console.anthropic.com/) で発行したキー |
| Claude Code CLI / Codex CLI / Gemini CLI | 各サービスの条件による | 各CLIのインストールとログイン |

**手軽なのはGemini APIキーです。** 「Gemini APIキーを設定…」を選んで貼り付けると、その場で使えるキーかどうかを確認します。CLIは、入っていれば自動で見つけます。

うまく繋がらない時は、メニューの「🔎 検出状況をコピー」を押してください。どこを探して何が見つからなかったか、AIが返したエラーそのものがクリップボードに入ります（**APIキー本体は含まれません**。設定済みかどうかだけです）。

### 3. 声を入れる（VOICEVOX）

[VOICEVOX](https://voicevox.hiroshiba.jp/)（無料）をインストールすると、「猫使ビィ」の声でおしゃべりします。

**Windows版には代替の音声がありません。** VOICEVOXを入れるまで、びくたんは声を出さず、吹き出しの文字だけで話します。

## 保存データと外部通信

- 設定・会話履歴・ことば帳・思い出帳・日記と、暗号化したClaude APIキーは、お使いのPC内の `%APPDATA%\bikunavi-desktop\state.json` に保存します。Claude APIキーの暗号化にはWindowsの安全な保存機能（DPAPI）を使います。
- Gemini APIキーは `%USERPROFILE%\.gemini\.env` に保存します。
- 音声入力は同梱のWhisperでローカル処理し、録音音声を外部へ送信しません。
- AI会話や自動セリフを利用すると、直近の会話、キャラクター設定への回答、ことば帳・思い出帳、取得済みニュース見出し等が、選択したAIサービスへプロンプトとして送られる場合があります。保存済み日記は初期状態では送らず、「日記を会話AIにも覚えさせる（送信）」をONにした場合だけ使います。
- クリップボード内容は、クリップボードについて明示的に質問した場合だけAIへ渡します。
- APIキーはメニューの「会話AI」→「Gemini APIキーを削除」「Claude APIキーを削除」で消去できます。
- 本アプリ独自の収集サーバーや広告・解析SDKはありません。

## Windows版で使えないもの

macOSの仕組みに依存している機能のため、Windows版では動きません。

- **VOICEVOX以外の読み上げ音声**。macOS版にはOS標準の代替音声がありますが、Windows版にはありません。
- 音声認識は、macOS版がApple Speechを優先するのに対し、Windows版は同梱のWhisperのみです。

再生中の曲に合わせた反応（曲に合わせて踊るなど）は、Windowsのメディア再生情報を通じてWindows版でも動きます。

## よくある操作

- **会話欄を開く**: `Ctrl + Shift + B`、またはメニューの「💬 会話欄を開く」
- **大きさを変える**: メニュー →「🖥 表示・サイズ」→ 極小 / 小 / 中 / 大
- **じゃまな時**: 「びくたん以外へクリックを通す」をONにすると、透明な部分が後ろのウィンドウへ素通しになります
- **パソコンを起動したら開く**: メニュー →「🖥 表示・サイズ」→「パソコンを起動したら開く」
- **終了**: メニューの一番下「終了」

## アンインストール

1. 通知領域のアイコンからメニューを開き、「終了」を選びます。
2. 展開したフォルダを削除します。
3. 設定や会話履歴も消す場合は、`%APPDATA%\bikunavi-desktop` フォルダを削除します。
4. Gemini APIキーも消す場合は、`%USERPROFILE%\.gemini\.env` の `GEMINI_API_KEY` の行を削除します。

エクスプローラーのアドレス欄に `%APPDATA%` や `%USERPROFILE%` と入力すると、そのフォルダを開けます。

## 利用条件

個人利用と、アプリが映ったスクリーンショット・配信画面の公開は無料です。アプリ・Live2Dモデルの再配布、モデルデータの抽出・流用、独自コードの転載・転用は許可していません。

`ELECTRON_LICENSE` と `LICENSES.chromium.html` はElectron・Chromiumに関するライセンスです。本アプリ全体をMITライセンスで提供するものではありません。詳しくは `LICENSE.md` と `THIRD_PARTY_NOTICES.md` を参照してください。

音声: VOICEVOX:猫使ビィ ／ Live2D Cubism Core © Live2D Inc. ／ ポモドーロジングルの音素材提供: [OtoLogic](https://otologic.jp/) (CC BY 4.0)
