# びくたん Desktop — はじめにお読みください

デスクトップの隅にびくたんが住みつき、話しかけると返してくれるアプリです。
ニュースを拾って独り言を言ったり、教えたことを覚えたり、決まった用事を
やってくれたりします。

## インストール

1. `びくたん.app` を「アプリケーション」フォルダへ移動します。
2. 一度起動を試します。
3. macOSに開発元未確認の警告が表示された場合は、「システム設定」→「プライバシーとセキュリティ」→「このまま開く」を選びます。

本アプリはAppleのDeveloper ID署名・公証を受けていません。Appleによるマルウェア確認済みという意味ではないため、必ず公式GitHub Releaseから取得し、Releaseに掲載されたSHA-256と一致することを確認してください。

「壊れているため開けません」と表示され、「このまま開く」でも起動できない場合に限り、内容と入手元を確認したうえで次を実行してください。

```bash
xattr -cr /Applications/びくたん.app
```

このコマンドはダウンロード由来の隔離属性を削除し、macOSの保護を一部迂回します。信頼できない場所から入手したアプリには実行しないでください。

## 最初にすること

### 1. メニューの開き方

**びくたんを右クリック**すると、その場にメニューが出ます。設定もお願いごとも、すべてここからです。

画面上端のメニューバーにある芽のアイコンからも、同じメニューを開けます。びくたんを閉じている時はこちらからしか開けません。

### 2. 会話AIを決める（これをしないと会話できません）

メニュー →「🤖 会話AI」

| 選べるもの | 費用 | 用意するもの |
|---|---|---|
| **Gemini APIキー** | 無料枠あり | [Google AI Studio](https://aistudio.google.com/apikey) で発行したキー |
| Claude APIキー | 有料 | [Anthropic Console](https://console.anthropic.com/) で発行したキー |
| Claude Code CLI / Codex CLI / Gemini CLI | 各サービスの条件による | 各CLIのインストールとログイン |

**手軽なのはGemini APIキーです。** 「Gemini APIキーを設定…」を選んで貼り付けると、その場で使えるキーかどうかを確認します。CLIは、入っていれば自動で見つけます。

### 3. 声を入れる（VOICEVOX）

[VOICEVOX](https://voicevox.hiroshiba.jp/) を入れて起動しておくと、びくたんが声で話します。入れなくても文字だけで使えます。

## お願いごと（決まった仕事を頼む）

話しかけるだけで、決まった仕事をやってもらえます。

```
あなた: 今日のメモ作って
びくたん: 「今日のデイリーメモを作る」ですね。やってしまっていいですか？
          ［お願い］［やめておく］
```

**最初は何も入っていません。** 自分で登録した人にだけ現れる機能です。

メニュー →「🗂 データ管理」→「🧹 お願いごとを設定する…」を選ぶと、雛形が入ったフォルダが開きます。

```
~/Library/Application Support/bikunavi-desktop/
├── errands.json    … 何を頼めるかを書く（これを編集します）
└── errands/        … 実行するものを置く場所
```

`errands.json` は `errands` フォルダの**ひとつ上**にあります。書き方は同梱の雛形と、`errands.json` の中のメモを見てください。

**実行できるのは `errands/` の中に自分で置いたものだけです。** 別の場所にあるものを頼みたい時は、コピーするかシンボリックリンクを張ってください。実行するたびに `errands/実行の記録.txt` へ残るので、後から確かめられます。

うっかり動かないよう、対象と動作の両方が言葉に含まれていて、頼む形になっている時だけ反応します。確認に曖昧な返事をした場合は実行しません。

## 保存データと外部通信

- 設定・会話履歴・ことば帳・思い出帳・日記と、暗号化したClaude APIキーは、利用者のMac内の `~/Library/Application Support/bikunavi-desktop/state.json` に保存します。Claude APIキーの暗号化にはmacOSの安全な保存機能（Keychain連動）を使います。
- 音声入力は同梱のWhisperでローカル処理し、録音音声を外部へ送信しません。
- AI会話や自動セリフを利用すると、直近の会話、キャラクター設定への回答、ことば帳・思い出帳、取得済みニュース見出し等が、選択したAIサービスへプロンプトとして送られる場合があります。保存済み日記は初期状態では送らず、「日記を会話AIにも覚えさせる（送信）」をONにした場合だけ使います。
- クリップボード内容は、クリップボードについて明示的に質問した場合だけAIへ渡します。
- Claude APIキーはメニューバーの「会話AI」→「Claude APIキーを削除」で消去できます。保存データをすべて初期化する場合は、びくたんを終了してから上記の `state.json` を削除します。
- 本アプリ独自の収集サーバーや広告・解析SDKはありません。

## 利用条件

個人利用と、アプリが映ったスクリーンショット・配信画面の公開は無料です。アプリ・Live2Dモデルの再配布、モデルデータの抽出・流用、独自コードの転載・転用は許可していません。

`ELECTRON_LICENSE` と `LICENSES.chromium.html` はElectron・Chromiumに関するライセンスです。本アプリ全体をMITライセンスで提供するものではありません。詳しくは `LICENSE.md` と `THIRD_PARTY_NOTICES.md` を参照してください。

音声: VOICEVOX:猫使ビィ ／ Live2D Cubism Core © Live2D Inc. ／ ポモドーロジングルの音素材提供: [OtoLogic](https://otologic.jp/) (CC BY 4.0)
